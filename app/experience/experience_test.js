'use strict';

describe('andrewDesmaraisApp.experience module', function() {

  beforeEach(module('andrewDesmaraisApp.experience'));

  describe('experience controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var experienceController = $controller('experienceController');
      expect(experienceController).toBeDefined();
    }));

  });
});